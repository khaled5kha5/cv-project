import 'dart:typed_data';

class PdfService {
  /// Generate a PDF from given data. Return PDF bytes.
  /// Implement with `pdf` package or similar when ready.
  Future<Uint8List> generatePdf(Map<String, dynamic> data) async {
    throw UnimplementedError('Implement PdfService.generatePdf using `pdf` package');
  }
}
